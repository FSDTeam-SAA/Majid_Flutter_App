import 'package:flutter/material.dart';

/// Fixed accent palette for the Transactions/Reports/Cash Management screens
/// — matches the client's reference design exactly, independent of the
/// app's normal theme accent.
abstract final class TransactionColors {
  static const green = Color(0xFF8DC63F);
  static const greenDark = Color(0xFF6FA82E);
  static const coral = Color(0xFFFF6B4A);
  static const blue = Color(0xFF4A90E2);
}
